import styled from 'styled-components'
import { th, variant, compose, system, breakpoints, color, backgroundColor } from '@xstyled/system'
import { getSystemPropTypes } from '@xstyled/prop-types'

export {
  styled,
  system,
  compose,
  th,
  variant,
  breakpoints,
  color,
  backgroundColor,
  getSystemPropTypes
}

export * from 'styled-components'
export * from '@xstyled/system'
export * from '@xstyled/prop-types'

export * from './stylize'
